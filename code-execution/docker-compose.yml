version: '3'
services:
  test:
    image: macscode/cpp-docker
    container_name: cpp_execution_environment
    volumes:
      - ./execution:/app/execution
    environment:
      - TEST_COUNT=2
    restart: "no"
